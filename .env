# ProjectHub Environment Configuration

# MongoDB Configuration
# For local MongoDB installation:
MONGODB_URI='mongodb+srv://nitpatel678:codeAlphaPassword@cluster0.vussvx4.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0'

# For MongoDB Atlas (replace with your connection string):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/projecthub?retryWrites=true&w=majority

# JWT Configuration
# IMPORTANT: Change this to a secure random string in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-long-and-random

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Enable debug logging
# DEBUG=*

# Optional: CORS Configuration (if needed for different domains)
# FRONTEND_URL=http://localhost:3000

# Optional: Email Configuration (for future email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Upload Configuration (for future file attachments)
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./uploads